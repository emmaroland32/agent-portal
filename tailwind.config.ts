import type { Config } from "tailwindcss"

const config = {
  // content: [] // Auto-detected in v4
  // darkMode: "class" // Handled in CSS via @custom-variant
} satisfies Config

export default config
